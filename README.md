# drone-track-server

to install, run:
```bash
$ npm install
```

to launch, run:
```bash
$ node server.js
```

to test, run:
```bash
$ jasmine-node .
```

To view, run the [drone-track-client](https://github.com/jbrecht/drone-track-client)
